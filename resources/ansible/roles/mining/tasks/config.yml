---
- name: Deploy xmrig config
  become: true
  ansible.builtin.template:
    src: xmrig-config.json.j2
    dest: "{{ xmrig_install_dir }}/config.json"
    owner: _xmrig
    group: staff
    mode: "0644"
  notify: restart xmrig
