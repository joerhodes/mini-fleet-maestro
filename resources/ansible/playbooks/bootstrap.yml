---
- name: Bootstrap Mac Mini
  hosts: all
  gather_facts: true

  tasks:
    - name: Apply assigned roles
      ansible.builtin.include_role:
        name: "{{ role_name }}"
      loop: "{{ app_roles }}"
      loop_control:
        loop_var: role_name
